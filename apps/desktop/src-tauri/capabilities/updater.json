{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Direct-update permissions for signed Linux AppImage builds",
  "windows": ["main"],
  "platforms": ["linux"],
  "permissions": [
    "process:allow-restart",
    "updater:default"
  ]
}
